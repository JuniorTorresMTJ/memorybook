# memorybook

Memorybook — a web application to create personalized memory books for loved ones.

This project uses:
- React + TypeScript + Vite
- Tailwind CSS
- Firebase (Auth, Firestore, Storage)

Getting started:

1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`
3. Configure Firebase using `.env` and the FIREBASE_SETUP.md guide

License: MIT
